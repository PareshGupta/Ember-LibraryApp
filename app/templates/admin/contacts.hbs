<h1>Contact List</h1>

<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>E-mail</th>
      <th>Message</th>
    </tr>
  </thead>
  <tbody>
  {{#each model as |contact|}}
    <tr>
      <th>{{contact.id}}</th>
      <td>{{contact.name}}</td>
      <td>{{contact.email}}</td>
      <td>{{contact.message}}</td>
    </tr>
  {{/each}}
  </tbody>
</table>
